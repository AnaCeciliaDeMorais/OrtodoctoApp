import 'package:flutter/material.dart';

class BankSlipDetailPage extends StatelessWidget {
  const BankSlipDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Bank Slip Detail Page'),
      ),
    );
  }
}