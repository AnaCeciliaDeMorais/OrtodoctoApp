import 'package:flutter/material.dart';

class StaffCreatePage extends StatelessWidget {
  const StaffCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Staff Create Page'),
      ),
    );
  }
}