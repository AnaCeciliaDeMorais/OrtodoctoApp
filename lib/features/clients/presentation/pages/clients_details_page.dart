import 'package:flutter/material.dart';

class ClientsDetailsPage extends StatelessWidget {
  const ClientsDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Clients Details Page'),
      ),
    );
  }
}